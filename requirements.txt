# Core Dependencies (Currently Used)
pydantic==2.11.1
PyYAML

# LLM Providers (Currently Used)
langchain-openai
langchain-google-genai

# Agent Framework (For future multi-agent workflows)
langgraph==0.6.8

# Utilities (Commonly needed)
numpy
requests==2.32.3

# API Framework (For web interface integration)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Vector Database (For AI agent data storage)
pinecone==6.0.0
openai>=1.12.0  # For embeddings

# Retrieval-Augmented Generation (RAG) Dependencies
# Note: PyMuPDF requires Visual Studio build tools. Install separately if needed:
# pip install PyMuPDF
# Note: scikit-learn requires C compiler. Use numpy alternatives or install separately if needed:
# pip install scikit-learn
python-dotenv==1.0.0  # For environment variable management

